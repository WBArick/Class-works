#include<stdio.h>
#define PI 3.1416
int main()
{
    double radius, area;
    printf("Enter the radius of the circle:");
    scanf("%lf",&radius);
    printf("Area of the circle = %.2f", area=PI*radius*radius);
    return 0;
}